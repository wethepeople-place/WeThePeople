# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Core dependencies
sqlalchemy>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# CLI and output
typer>=0.9.0
rich>=13.0.0

# Reliability
tenacity>=8.2.0

# Validation
pydantic>=2.0.0
pydantic[email]>=2.0.0  # EmailStr support

# Authentication
python-jose[cryptography]>=3.3.0  # JWT token signing/verification
passlib[bcrypt]>=1.7.4  # Password hashing with bcrypt
bcrypt>=4.0,<5  # passlib 1.7.4 is incompatible with bcrypt 5.x

# Caching
diskcache>=5.6.0
cachetools>=5.3.0

# Database
alembic>=1.12.0
# psycopg2-binary>=2.9.0  # Optional: PostgreSQL driver (only needed if using PostgreSQL instead of SQLite)

# Rate Limiting
slowapi>=0.1.9

# Scheduling
fastscheduler>=0.1.0

# PDF parsing
pdfplumber>=0.10.0
cairosvg>=2.7.0  # OG image generation (SVG to PNG)
# docling>=2.0.0  # Optional: OCR for scanned PDFs. Requires PyTorch (~15GB). Install separately if needed.

# Twitter/X API
tweepy>=4.14.0

# Optional: API clients
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
