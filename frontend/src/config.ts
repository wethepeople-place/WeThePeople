/** App-wide constants — single source of truth for values used in multiple places. */

/** Date shown on legal pages (Terms, Privacy, Disclaimer). Update when policies change. */
export const LEGAL_LAST_UPDATED = 'August 24, 2026';

/** Press tier pricing shown on Account page. */
export const PRESS_TIER_PRICE = '$29/mo';
